<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imnoob.community.mapper.CommentMapper">
    <insert id="insertComment" parameterType="Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_comment(parent_id,type,user_id,gmt_create,gmt_modified,like_count,content.comment_count)  values ( #{parentId,jdbcType=BIGINT}, #{type,jdbcType=INTEGER},
        #{commentator,jdbcType=BIGINT}, #{gmtCreate,jdbcType=BIGINT}, #{gmtModified,jdbcType=BIGINT},
        #{likeCount,jdbcType=BIGINT}, #{content,jdbcType=VARCHAR}, #{commentCount,jdbcType=INTEGER},
        #{commentCount,jdbcType=INTEGER}
        )
    </insert>


</mapper>